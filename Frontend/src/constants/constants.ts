export const DOMAIN = 'localhost';
export const API_PORT = 5000;
export const WS_PORT = 8080;
export const BASE_URL = `http://${DOMAIN}:${API_PORT}`;
export const WS_URL = `ws://${DOMAIN}:${WS_PORT}`;
